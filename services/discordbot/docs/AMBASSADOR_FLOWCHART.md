# Ambassador System — Complete Flowchart

> **Companion Document to:** `AMBASSADOR_SYSTEM.md`  
> **Visual representation of every system component and workflow**

---

## 🌳 MASTER SYSTEM TREE

```
                                    ┌─────────────────────────────────────┐
                                    │      AMBASSADOR SYSTEM ROOT         │
                                    │   "Converting Free → Paid Members"  │
                                    └──────────────────┬──────────────────┘
                                                       │
            ┌──────────────────────────────────────────┼──────────────────────────────────────────┐
            │                                          │                                          │
            ▼                                          ▼                                          ▼
┌───────────────────────┐               ┌───────────────────────┐               ┌───────────────────────┐
│   🏛️ INFRASTRUCTURE   │               │   👥 USER JOURNEYS    │               │   📊 DATA LAYER       │
│                       │               │                       │               │                       │
│ • 2 Discord Servers   │               │ • Waitlist → Apply    │               │ • PostgreSQL DB       │
│ • Bot in Both         │               │ • Onboard → Interview │               │ • 7 Tables            │
│ • Role Hierarchies    │               │ • Accept → Ambassador │               │ • Event Logging       │
│ • Channel Permissions │               │ • Claim → Convert     │               │ • State Tracking      │
└───────────────────────┘               │ • Payout → Withdraw   │               └───────────────────────┘
                                        └───────────────────────┘
```

---

## 🏛️ INFRASTRUCTURE TREE

```
INFRASTRUCTURE
│
├─── 🌐 SERVERS
│    │
│    ├─── MAIN SERVER (1431718856619200686)
│    │    │
│    │    ├─── PURPOSE: Public community, claiming happens here
│    │    │
│    │    ├─── ROLES
│    │    │    ├─── Ambassador ────────────── [TO CREATE] Cross-server ID
│    │    │    ├─── Lifetime Member ───────── [718643316786462772] ❌ NOT CLAIMABLE
│    │    │    ├─── Paying Member ─────────── [885910828086362132] ❌ NOT CLAIMABLE
│    │    │    └─── Default Member ────────── [718643370301325404] ✅ CLAIMABLE
│    │    │
│    │    ├─── COMMANDS AVAILABLE
│    │    │    ├─── /claim <user> ─────────── Ambassador only
│    │    │    └─── /unclaim <user> ───────── Ambassador only
│    │    │
│    │    └─── EVENTS MONITORED
│    │         └─── GuildMemberUpdate ─────── Conversion detection
│    │
│    └─── AMBASSADOR SERVER (1441909666308423874)
│         │
│         ├─── PURPOSE: Private hub, management, payouts
│         │
│         ├─── ROLES
│         │    ├─── Staff/Admin ───────────── [1441909798852366499] Full access
│         │    ├─── Full Ambassador ───────── [1445251690365456466] Accepted members
│         │    └─── Approved Owl ──────────── [1445249241743364236] Pending interview
│         │
│         ├─── CHANNELS
│         │    ├─── Approved Applicants ───── [1445245329778806834] Interview threads
│         │    ├─── Private Channels Cat ──── [1445215664515055779] :bell:│[name]
│         │    ├─── Claim Log ─────────────── [1445215326550622419] Admin only
│         │    └─── Conversion Log ────────── [1445214943841615922] Admin only
│         │
│         └─── COMMANDS AVAILABLE
│              ├─── ADMIN ONLY
│              │    ├─── /wait-list
│              │    ├─── /owls-vision
│              │    ├─── /onboard <user>
│              │    ├─── /leaderboard
│              │    ├─── /panel
│              │    ├─── /balance-add <user> <amount>
│              │    ├─── /balance-remove <user> <amount>
│              │    └─── /ambassador-stats <user>
│              │
│              └─── AMBASSADOR ONLY
│                   ├─── /my-stats
│                   ├─── /my-claims
│                   └─── /request-payout
│
├─── 🤖 BOT REQUIREMENTS
│    │
│    ├─── Must be in BOTH servers simultaneously
│    ├─── Must be HIGHEST role in hierarchy (both servers)
│    │
│    ├─── INTENTS REQUIRED
│    │    ├─── Guilds
│    │    ├─── GuildMembers ────────────────── For role change detection
│    │    └─── GuildMessages
│    │
│    └─── PERMISSIONS
│         ├─── Administrator (preferred)
│         └─── OR: Manage Channels, Manage Roles, Send Messages,
│                  Create Threads, Manage Threads, Embed Links, Attach Files
│
└─── 🔗 CROSS-SERVER SYNC
     │
     ├─── When ambassador ACCEPTED:
     │    ├─── Grant Full Ambassador role (Amb Server)
     │    └─── Grant Ambassador role (Main Server)
     │
     ├─── When checking /claim:
     │    ├─── Verify ambassador exists (Amb Server DB)
     │    └─── Verify target user roles (Main Server)
     │
     └─── When conversion detected:
          ├─── Update DB (Amb Server operations)
          ├─── Notify private channel (Amb Server)
          └─── Log conversion (Amb Server)
```

---

## 📊 DATABASE TREE

```
DATABASE (PostgreSQL)
│
├─── 📋 ambassador_applicants
│    │
│    │   Stores users approved via /onboard, pending interview
│    │
│    ├─── id ──────────────────── SERIAL PRIMARY KEY
│    ├─── user_id ─────────────── VARCHAR(20) UNIQUE, Discord ID
│    ├─── username ────────────── VARCHAR(100)
│    ├─── display_name ────────── VARCHAR(100)
│    ├─── approved_by ─────────── VARCHAR(20) Admin who ran /onboard
│    ├─── approved_at ─────────── TIMESTAMP
│    ├─── thread_id ───────────── VARCHAR(20) Interview thread
│    ├─── status ──────────────── VARCHAR(20) [pending|accepted|rejected]
│    └─── notes ───────────────── TEXT
│
├─── 👤 ambassadors
│    │
│    │   Fully accepted ambassadors with stats and balance
│    │
│    ├─── IDENTITY
│    │    ├─── id ─────────────── SERIAL PRIMARY KEY
│    │    ├─── user_id ────────── VARCHAR(20) UNIQUE
│    │    ├─── username ───────── VARCHAR(100)
│    │    ├─── display_name ───── VARCHAR(100)
│    │    └─── private_channel_id VARCHAR(20) Their :bell:│[name]
│    │
│    ├─── TIMESTAMPS
│    │    ├─── accepted_at ────── TIMESTAMP
│    │    └─── accepted_by ────── VARCHAR(20)
│    │
│    ├─── STATS
│    │    ├─── total_claims ───── INT [0] All-time claims made
│    │    ├─── total_unclaims ─── INT [0] All-time unclaims
│    │    ├─── total_conversions  INT [0] Weekly (resets)
│    │    ├─── lifetime_conversions INT [0] Never resets
│    │    └─── current_claims ─── INT [0] Active (max 3)
│    │
│    ├─── BALANCE
│    │    ├─── balance ────────── DECIMAL(10,2) [0.00] Current
│    │    ├─── total_earned ───── DECIMAL(10,2) [0.00] Lifetime
│    │    └─── total_paid_out ─── DECIMAL(10,2) [0.00] Lifetime
│    │
│    ├─── TIER
│    │    └─── tier ───────────── VARCHAR(20) [bronze|silver|gold|platinum]
│    │
│    ├─── COOLDOWNS
│    │    └─── last_unclaim_at ── TIMESTAMP (1hr cooldown)
│    │
│    └─── STATUS
│         └─── is_active ──────── BOOLEAN [true]
│
├─── 🎯 claims
│    │
│    │   All claim relationships between ambassadors and users
│    │
│    ├─── id ──────────────────── SERIAL PRIMARY KEY
│    ├─── ambassador_id ───────── VARCHAR(20)
│    ├─── claimed_user_id ─────── VARCHAR(20)
│    ├─── claimed_username ────── VARCHAR(100)
│    ├─── claimed_at ──────────── TIMESTAMP
│    ├─── status ──────────────── VARCHAR(20) [active|converted|unclaimed]
│    ├─── converted_at ────────── TIMESTAMP (if converted)
│    └─── unclaimed_at ────────── TIMESTAMP (if unclaimed)
│    │
│    └─── CONSTRAINT: UNIQUE(ambassador_id, claimed_user_id)
│                     Can never claim same person twice
│
├─── 💰 conversions
│    │
│    │   Historical log of all conversions
│    │
│    ├─── id ──────────────────── SERIAL PRIMARY KEY
│    ├─── ambassador_id ───────── VARCHAR(20)
│    ├─── converted_user_id ───── VARCHAR(20)
│    ├─── converted_username ──── VARCHAR(100)
│    ├─── converted_at ────────── TIMESTAMP
│    ├─── commission ──────────── DECIMAL(10,2) Amount earned
│    ├─── claim_id ────────────── INT FK → claims(id)
│    ├─── week_number ─────────── INT For weekly leaderboard
│    └─── year ────────────────── INT
│
├─── 💸 payout_requests
│    │
│    │   Payout requests and their status
│    │
│    ├─── id ──────────────────── SERIAL PRIMARY KEY
│    ├─── ambassador_id ───────── VARCHAR(20)
│    ├─── amount ──────────────── DECIMAL(10,2) Requested
│    ├─── payment_method ──────── VARCHAR(100) e.g., PayPal email
│    ├─── thread_id ───────────── VARCHAR(20) Payout thread
│    ├─── status ──────────────── VARCHAR(20) [pending|completed|cancelled]
│    ├─── requested_at ────────── TIMESTAMP
│    ├─── completed_at ────────── TIMESTAMP
│    ├─── completed_by ────────── VARCHAR(20) Admin who processed
│    ├─── actual_amount ───────── DECIMAL(10,2) What was paid
│    └─── notes ───────────────── TEXT
│
├─── 📝 balance_transactions
│    │
│    │   Audit log of ALL balance changes
│    │
│    ├─── id ──────────────────── SERIAL PRIMARY KEY
│    ├─── ambassador_id ───────── VARCHAR(20)
│    ├─── type ────────────────── VARCHAR(20) [conversion|payout|bonus|adjustment]
│    ├─── amount ──────────────── DECIMAL(10,2) +credit/-debit
│    ├─── balance_after ───────── DECIMAL(10,2)
│    ├─── description ─────────── TEXT
│    ├─── performed_by ────────── VARCHAR(20) Admin (for manual)
│    └─── created_at ──────────── TIMESTAMP
│
└─── 🏆 weekly_leaderboard
     │
     │   Snapshot of weekly stats (resets Sunday midnight UTC)
     │
     ├─── id ──────────────────── SERIAL PRIMARY KEY
     ├─── ambassador_id ───────── VARCHAR(20)
     ├─── week_number ─────────── INT
     ├─── year ────────────────── INT
     └─── conversions ─────────── INT [0]
     │
     └─── CONSTRAINT: UNIQUE(ambassador_id, week_number, year)
```

---

## 👥 USER JOURNEY FLOWCHARTS

### 🚀 COMPLETE APPLICANT JOURNEY

```
                                         ┌─────────────────┐
                                         │  RANDOM USER    │
                                         │  (Free Member)  │
                                         └────────┬────────┘
                                                  │
                                                  ▼
                              ┌─────────────────────────────────────┐
                              │         DISCOVERY PHASE             │
                              │                                     │
                              │  User finds waitlist channel in     │
                              │  Ambassador Server (public view)    │
                              └──────────────────┬──────────────────┘
                                                 │
                                                 ▼
                              ┌─────────────────────────────────────┐
                              │     /wait-list EMBED DISPLAYED      │
                              │                                     │
                              │  ┌─────────────────────────────┐    │
                              │  │ [waitlist.png] BANNER       │    │
                              │  ├─────────────────────────────┤    │
                              │  │ Information about program   │    │
                              │  │ Benefits, requirements      │ [📝 Apply Now] ←─── Link Button
                              │  ├─────────────────────────────┤    │        │
                              │  │ [footer.png] FOOTER         │    │        │
                              │  └─────────────────────────────┘    │        │
                              └──────────────────┬──────────────────┘        │
                                                 │                           │
                                                 │                           ▼
                                                 │               ┌───────────────────────┐
                                                 │               │     TALLY.COM         │
                                                 │               │   (External Form)     │
                                                 │               │                       │
                                                 │               │  User fills out       │
                                                 │               │  application form     │
                                                 │               └───────────┬───────────┘
                                                 │                           │
                                                 ▼                           ▼
                              ┌─────────────────────────────────────────────────────────┐
                              │                   MANUAL REVIEW PHASE                    │
                              │                                                          │
                              │   Admin reviews Tally submissions externally             │
                              │   Decision: Approve or Ignore                            │
                              │                                                          │
                              └─────────────────────────┬────────────────────────────────┘
                                                        │
                                        ┌───────────────┴───────────────┐
                                        │                               │
                                        ▼                               ▼
                               ┌────────────────┐              ┌────────────────┐
                               │    IGNORED     │              │    APPROVED    │
                               │                │              │                │
                               │  No action     │              │  Admin runs:   │
                               │  User stays    │              │  /onboard @usr │
                               │  as free       │              │                │
                               └────────────────┘              └───────┬────────┘
                                                                       │
                                                                       ▼
                                             ┌───────────────────────────────────────────┐
                                             │           /ONBOARD EXECUTION              │
                                             └───────────────────────────────────────────┘
                                                               │
                                                               │ (See detailed flow below)
                                                               ▼
```

---

### 📋 /ONBOARD COMMAND — DETAILED FLOW

```
/onboard @User
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              VALIDATION PHASE                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── CHECK 1: Is executor Staff/Admin?
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "Only administrators can use this command"
      │     │          └─── END
      │     │
      │     └─── YES ─▶ CONTINUE
      │
      ├──── CHECK 2: Is command in Ambassador Server?
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "This command only works in Ambassador Server"
      │     │          └─── END
      │     │
      │     └─── YES ─▶ CONTINUE
      │
      ├──── CHECK 3: Does target user exist in server?
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "User not found in this server"
      │     │          └─── END
      │     │
      │     └─── YES ─▶ CONTINUE
      │
      ├──── CHECK 4: Is user already an applicant?
      │     │         (Query: ambassador_applicants WHERE user_id = ?)
      │     │
      │     ├─── YES ─▶ ❌ EPHEMERAL: "User already has a pending application"
      │     │          └─── END
      │     │
      │     └─── NO ──▶ CONTINUE
      │
      └──── CHECK 5: Is user already an ambassador?
            │         (Query: ambassadors WHERE user_id = ? AND is_active = true)
            │
            ├─── YES ─▶ ❌ EPHEMERAL: "User is already an ambassador"
            │          └─── END
            │
            └─── NO ──▶ PROCEED TO EXECUTION
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              EXECUTION PHASE                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Database Insert
      │  ─────────────────────────
      │  INSERT INTO ambassador_applicants (
      │      user_id, username, display_name,
      │      approved_by, status
      │  ) VALUES (?, ?, ?, admin_id, 'pending')
      │
      ▼
      │  STEP 2: Grant Approved Owl Role
      │  ─────────────────────────────────
      │  s.GuildMemberRoleAdd(guild, user_id, "1445249241743364236")
      │
      ▼
      │  STEP 3: Hide Waitlist Channel
      │  ──────────────────────────────
      │  Update channel permission overwrite:
      │  Approved Owl role → ViewChannel: false
      │
      ▼
      │  STEP 4: Create Private Thread
      │  ──────────────────────────────
      │  s.ThreadStartComplex(
      │      ChannelID: "1445245329778806834",  // Approved Applicants
      │      ThreadStart{
      │          Name: "Interview | {username}",
      │          Type: GuildPrivateThread,
      │          Invitable: false,
      │          AutoArchiveDuration: 10080  // 7 days
      │      }
      │  )
      │
      ▼
      │  STEP 5: Add Members to Thread
      │  ──────────────────────────────
      │  s.ThreadMemberAdd(thread_id, user_id)
      │  s.ThreadMemberAdd(thread_id, admin_id)
      │  // Staff role members auto-see private threads
      │
      ▼
      │  STEP 6: Update Database with Thread ID
      │  ────────────────────────────────────────
      │  UPDATE ambassador_applicants
      │  SET thread_id = ?
      │  WHERE user_id = ?
      │
      ▼
      │  STEP 7: Post Interview Embed in Thread
      │  ───────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────┐
      │  │  📋 Ambassador Application — @Username                  │
      │  │                                                         │
      │  │  Welcome to your interview! A staff member will be     │
      │  │  with you shortly to discuss your application.         │
      │  │                                                         │
      │  │  **Application Status:** 🟡 Pending Review              │
      │  │                                                         │
      │  │  ─────────────────────────────────────                  │
      │  │                                                         │
      │  │  ⚠️ **For Staff Only:**                                 │
      │  │                                                         │
      │  │  ┌─────────────┐  ┌─────────────┐                       │
      │  │  │ ✅ Accept   │  │ ❌ Reject   │                       │
      │  │  └─────────────┘  └─────────────┘                       │
      │  │                                                         │
      │  │  CustomIDs:                                             │
      │  │  • Interview:Accept                                     │
      │  │  • Interview:Reject                                     │
      │  └─────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 8: Respond to Admin
      │  ─────────────────────────
      │  EPHEMERAL: "✅ Onboarding initiated for @User. Interview thread created."
      │
      ▼
      │  STEP 9: Log to Conversion Log (Optional)
      │  ─────────────────────────────────────────
      │  Post embed to channel 1445214943841615922:
      │  "📝 New Applicant: @User approved for interview by @Admin"
      │
      └─────▶ END (Waiting for interview decision)
```

---

### ✅❌ INTERVIEW DECISION FLOW

```
                         ┌──────────────────────────────────┐
                         │     INTERVIEW IN PROGRESS        │
                         │                                  │
                         │  Staff talks with applicant      │
                         │  in private thread               │
                         └───────────────┬──────────────────┘
                                         │
                                         │ Staff clicks button
                                         │
                         ┌───────────────┴───────────────┐
                         │                               │
                         ▼                               ▼
              ┌──────────────────┐            ┌──────────────────┐
              │  ❌ REJECT BTN   │            │  ✅ ACCEPT BTN   │
              │                  │            │                  │
              │  CustomID:       │            │  CustomID:       │
              │  Interview:Reject│            │  Interview:Accept│
              └────────┬─────────┘            └────────┬─────────┘
                       │                               │
                       ▼                               ▼
              ┌──────────────────────────────────────────────────────┐
              │                 CONFIRMATION MODAL                    │
              │                                                       │
              │  "Are you sure you want to [reject/accept] @User?"   │
              │                                                       │
              │  ┌─────────────┐  ┌─────────────┐                     │
              │  │ ✅ Confirm  │  │ ❌ Cancel   │                     │
              │  └─────────────┘  └─────────────┘                     │
              └───────────────────────┬───────────────────────────────┘
                                      │
                      ┌───────────────┴───────────────┐
                      │ CANCEL                        │ CONFIRM
                      ▼                               ▼
              ┌──────────────┐               ┌───────────────────┐
              │   CANCELLED  │               │  PROCEED WITH     │
              │              │               │  DECISION         │
              │  "Action     │               │                   │
              │  cancelled"  │               │                   │
              │              │               │                   │
              │  END         │               │                   │
              └──────────────┘               └─────────┬─────────┘
                                                       │
                                       ┌───────────────┴───────────────┐
                                       │                               │
                                       ▼                               ▼

══════════════════════════════════════════════════════════════════════════════
                              REJECTION PATH
══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                           REJECTION EXECUTION                                │
└─────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Update Database
      │  ─────────────────────────
      │  UPDATE ambassador_applicants
      │  SET status = 'rejected'
      │  WHERE user_id = ?
      │
      ▼
      │  STEP 2: Remove Approved Owl Role
      │  ──────────────────────────────────
      │  s.GuildMemberRoleRemove(guild, user_id, "1445249241743364236")
      │
      ▼
      │  STEP 3: DELETE Thread (Not Archive!)
      │  ─────────────────────────────────────
      │  s.ChannelDelete(thread_id)
      │
      ▼
      │  STEP 4: Optional - DM User
      │  ───────────────────────────
      │  "Your ambassador application has been reviewed.
      │   Unfortunately, we are unable to proceed at this time.
      │   Feel free to apply again in the future."
      │
      ▼
      │  STEP 5: Log to Conversion Log
      │  ───────────────────────────────
      │  Post to 1445214943841615922:
      │  "❌ Application Rejected: @User by @Admin"
      │
      └─────▶ END (User returns to normal state)


══════════════════════════════════════════════════════════════════════════════
                              ACCEPTANCE PATH
══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│                           ACCEPTANCE EXECUTION                               │
└─────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Update Applicants Table
      │  ─────────────────────────────────
      │  UPDATE ambassador_applicants
      │  SET status = 'accepted'
      │  WHERE user_id = ?
      │
      ▼
      │  STEP 2: DELETE Interview Thread
      │  ─────────────────────────────────
      │  s.ChannelDelete(thread_id)
      │
      ▼
      │  STEP 3: Remove Approved Owl Role (Amb Server)
      │  ───────────────────────────────────────────────
      │  s.GuildMemberRoleRemove(amb_guild, user_id, "1445249241743364236")
      │
      ▼
      │  STEP 4: Grant Full Ambassador Role (Amb Server)
      │  ─────────────────────────────────────────────────
      │  s.GuildMemberRoleAdd(amb_guild, user_id, "1445251690365456466")
      │
      ▼
      │  STEP 5: Grant Ambassador Role (MAIN SERVER - Cross-Server!)
      │  ─────────────────────────────────────────────────────────────
      │  s.GuildMemberRoleAdd("1431718856619200686", user_id, main_ambassador_role)
      │
      │  ⚠️ CRITICAL: Bot must be in Main Server with Manage Roles permission
      │
      ▼
      │  STEP 6: Wait 5 Seconds
      │  ───────────────────────
      │  time.Sleep(5 * time.Second)
      │
      │  Why? Gives Discord time to sync role changes,
      │       prevents rate limits, smooth UX
      │
      ▼
      │  STEP 7: Create Private Channel
      │  ────────────────────────────────
      │  s.GuildChannelCreateComplex(amb_guild, GuildChannelCreateData{
      │      Name: "🔔｜{username}",
      │      Type: GuildText,
      │      ParentID: "1445215664515055779",  // Private Channels Category
      │      PermissionOverwrites: [
      │          { @everyone: ViewChannel: false },
      │          { user_id: ViewChannel: true, SendMessages: true },
      │          { staff_role: ViewChannel: true, SendMessages: true }
      │      ]
      │  })
      │
      ▼
      │  STEP 8: INSERT into Ambassadors Table
      │  ───────────────────────────────────────
      │  INSERT INTO ambassadors (
      │      user_id, username, display_name,
      │      private_channel_id, accepted_at, accepted_by,
      │      tier, is_active
      │  ) VALUES (?, ?, ?, channel_id, NOW(), admin_id, 'bronze', true)
      │
      ▼
      │  STEP 9: Post Welcome Embed in Private Channel
      │  ───────────────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │ [ambassadorwelcome.png] BANNER                                  │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │                                                                 │
      │  │  # 🎉 Welcome to the Team, @Username!                          │
      │  │                                                                 │
      │  │  Congratulations! You've officially joined...                   │
      │  │                                                                 │
      │  │  **Getting Started:**                                          │
      │  │  1️⃣ Head to Main Server to start claiming...                   │
      │  │  2️⃣ Guide your claimed members...                              │
      │  │  3️⃣ Track your progress...                                     │
      │  │                                                                 │
      │  │  **Your Limits:**                                              │
      │  │  • 3 active claims                                             │
      │  │  • 1 unclaim per hour                                          │
      │  │  • Payouts after 2 conversions                                 │
      │  │                                                                 │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │ [footer.png] FOOTER                                            │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 10: Post APPROVED Embed (Separate Message)
      │  ─────────────────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │ [approved.png] BANNER                                          │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │                                                                 │
      │  │  🎊 **APPLICATION APPROVED**                                    │
      │  │                                                                 │
      │  │  You are now an official Owls Capital Ambassador!              │
      │  │                                                                 │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │ [footer.png] FOOTER                                            │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 11: Log to Conversion Log
      │  ────────────────────────────────
      │  Post to 1445214943841615922:
      │  "✅ **NEW AMBASSADOR**: @User accepted by @Admin"
      │
      └─────▶ END (User is now a full ambassador!)
```

---

## 🎯 CLAIMS SYSTEM FLOWCHART

### /claim @user — COMPLETE FLOW

```
/claim @TargetUser
      │
      │ Executed in: MAIN SERVER (1431718856619200686) ONLY
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              VALIDATION GAUNTLET                                     │
│                         (All checks must pass to proceed)                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │
      ├──── CHECK 1: CORRECT SERVER?
      │     │
      │     │ if i.GuildID != "1431718856619200686"
      │     │
      │     ├─── WRONG SERVER ──▶ ❌ EPHEMERAL: "This command can only be used
      │     │                                    in the Main Server."
      │     │                     └─── END
      │     │
      │     └─── CORRECT ────────▶ CONTINUE
      │
      │
      ├──── CHECK 2: IS EXECUTOR AN AMBASSADOR?
      │     │
      │     │ Query: SELECT * FROM ambassadors
      │     │        WHERE user_id = executor_id AND is_active = true
      │     │
      │     │ Also check: Has Ambassador role in Main Server
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "You are not an active ambassador."
      │     │          └─── END
      │     │
      │     └─── YES ─▶ CONTINUE
      │
      │
      ├──── CHECK 3: CURRENT CLAIMS < 3?
      │     │
      │     │ Query: SELECT current_claims FROM ambassadors
      │     │        WHERE user_id = executor_id
      │     │
      │     ├─── current_claims >= 3
      │     │    │
      │     │    └──▶ ❌ EPHEMERAL: "You've reached your maximum of 3 active claims.
      │     │                       Unclaim someone or wait for a conversion."
      │     │         └─── END
      │     │
      │     └─── current_claims < 3 ──▶ CONTINUE
      │
      │
      ├──── CHECK 4: TARGET IS NOT SELF?
      │     │
      │     │ if target_user_id == executor_id
      │     │
      │     ├─── SAME ──▶ ❌ EPHEMERAL: "You cannot claim yourself."
      │     │             └─── END
      │     │
      │     └─── DIFFERENT ─▶ CONTINUE
      │
      │
      ├──── CHECK 5: TARGET HAS PAYING ROLE?
      │     │
      │     │ Check target's roles in Main Server:
      │     │ • Paying Member (885910828086362132)
      │     │ • Lifetime Member (718643316786462772)
      │     │
      │     ├─── HAS PAYING ROLE
      │     │    │
      │     │    └──▶ ❌ EPHEMERAL: "This user is already a paying member
      │     │                       and cannot be claimed."
      │     │         └─── END
      │     │
      │     └─── NO PAYING ROLE ─▶ CONTINUE
      │
      │
      ├──── CHECK 6: TARGET IS AN AMBASSADOR?
      │     │
      │     │ Check: Has Ambassador role OR exists in ambassadors table
      │     │
      │     ├─── IS AMBASSADOR
      │     │    │
      │     │    └──▶ ❌ EPHEMERAL: "You cannot claim another ambassador."
      │     │         └─── END
      │     │
      │     └─── NOT AMBASSADOR ─▶ CONTINUE
      │
      │
      ├──── CHECK 7: ALREADY CLAIMED BY THIS AMBASSADOR BEFORE?
      │     │
      │     │ Query: SELECT * FROM claims
      │     │        WHERE ambassador_id = executor_id
      │     │        AND claimed_user_id = target_id
      │     │
      │     │ Note: Checks ALL statuses (active, converted, unclaimed)
      │     │       You can NEVER claim the same person twice, ever.
      │     │
      │     ├─── EXISTS ──▶ ❌ EPHEMERAL: "You have already claimed this user
      │     │                             before. You cannot claim them again."
      │     │               └─── END
      │     │
      │     └─── NOT EXISTS ─▶ CONTINUE
      │
      │
      └──── CHECK 8: TARGET CURRENTLY CLAIMED BY ANYONE?
            │
            │ Query: SELECT * FROM claims
            │        WHERE claimed_user_id = target_id
            │        AND status = 'active'
            │
            ├─── CURRENTLY CLAIMED
            │    │
            │    └──▶ ❌ EPHEMERAL: "This user is already claimed by
            │                       another ambassador."
            │         └─── END
            │
            └─── NOT CLAIMED ─▶ ✅ ALL CHECKS PASSED!
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              CLAIM EXECUTION                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Insert Claim Record
      │  ─────────────────────────────
      │  INSERT INTO claims (
      │      ambassador_id,
      │      claimed_user_id,
      │      claimed_username,
      │      claimed_at,
      │      status
      │  ) VALUES (
      │      executor_id,
      │      target_id,
      │      target_username,
      │      NOW(),
      │      'active'
      │  )
      │
      ▼
      │  STEP 2: Update Ambassador Stats
      │  ─────────────────────────────────
      │  UPDATE ambassadors SET
      │      current_claims = current_claims + 1,
      │      total_claims = total_claims + 1
      │  WHERE user_id = executor_id
      │
      ▼
      │  STEP 3: Log to Claim Log Channel
      │  ──────────────────────────────────
      │  Post embed to 1445215326550622419:
      │
      │  ┌─────────────────────────────────────────┐
      │  │ 📥 **CLAIM LOGGED**                     │
      │  │                                         │
      │  │ **Ambassador:** @Executor (ID)          │
      │  │ **Claimed User:** @Target (ID)          │
      │  │ **Active Claims:** 2/3                  │
      │  │ **Timestamp:** <t:unix:F>               │
      │  │                                         │
      │  │ Color: Green (#00FF00)                  │
      │  └─────────────────────────────────────────┘
      │
      ▼
      │  STEP 4: Respond to Ambassador
      │  ───────────────────────────────
      │  EPHEMERAL: "✅ Successfully claimed @TargetUser!
      │              
      │              You now have X/3 active claims.
      │              Guide them well — when they upgrade,
      │              you earn commission!"
      │
      └─────▶ END (Claim is now active, waiting for conversion)
```

---

### /unclaim @user — COMPLETE FLOW

```
/unclaim @TargetUser
      │
      │ Executed in: MAIN SERVER (1431718856619200686) ONLY
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              VALIDATION CHECKS                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── CHECK 1: CORRECT SERVER?
      │     │
      │     ├─── WRONG ──▶ ❌ EPHEMERAL: "This command can only be used
      │     │                            in the Main Server."
      │     └─── CORRECT ─▶ CONTINUE
      │
      ├──── CHECK 2: IS EXECUTOR AN AMBASSADOR?
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "You are not an active ambassador."
      │     └─── YES ─▶ CONTINUE
      │
      ├──── CHECK 3: DOES AMBASSADOR HAVE THIS USER CLAIMED?
      │     │
      │     │ Query: SELECT * FROM claims
      │     │        WHERE ambassador_id = executor_id
      │     │        AND claimed_user_id = target_id
      │     │        AND status = 'active'
      │     │
      │     ├─── NOT FOUND ──▶ ❌ EPHEMERAL: "You don't have this user claimed."
      │     └─── FOUND ─────▶ CONTINUE
      │
      └──── CHECK 4: UNCLAIM COOLDOWN CHECK
            │
            │ Query: SELECT last_unclaim_at FROM ambassadors
            │        WHERE user_id = executor_id
            │
            │ Calculate: NOW() - last_unclaim_at
            │
            ├─── < 1 HOUR
            │    │
            │    │ minutes_remaining = 60 - minutes_elapsed
            │    │
            │    └──▶ ❌ EPHEMERAL: "You're on cooldown. You can unclaim
            │                       again in {minutes_remaining} minutes."
            │         └─── END
            │
            └─── >= 1 HOUR (or never unclaimed) ──▶ ✅ PROCEED
                                                        │
                                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              UNCLAIM EXECUTION                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Update Claim Status
      │  ─────────────────────────────
      │  UPDATE claims SET
      │      status = 'unclaimed',
      │      unclaimed_at = NOW()
      │  WHERE ambassador_id = executor_id
      │  AND claimed_user_id = target_id
      │  AND status = 'active'
      │
      ▼
      │  STEP 2: Update Ambassador Stats
      │  ─────────────────────────────────
      │  UPDATE ambassadors SET
      │      current_claims = current_claims - 1,
      │      total_unclaims = total_unclaims + 1,
      │      last_unclaim_at = NOW()
      │  WHERE user_id = executor_id
      │
      ▼
      │  STEP 3: Log to Claim Log Channel
      │  ──────────────────────────────────
      │  Post embed to 1445215326550622419:
      │
      │  ┌─────────────────────────────────────────┐
      │  │ 📤 **UNCLAIM LOGGED**                   │
      │  │                                         │
      │  │ **Ambassador:** @Executor (ID)          │
      │  │ **Unclaimed User:** @Target (ID)        │
      │  │ **Claims Remaining:** 1/3               │
      │  │ **Timestamp:** <t:unix:F>               │
      │  │                                         │
      │  │ Color: Orange (#FFA500)                 │
      │  └─────────────────────────────────────────┘
      │
      ▼
      │  STEP 4: Respond to Ambassador
      │  ───────────────────────────────
      │  EPHEMERAL: "✅ You've unclaimed @TargetUser.
      │              
      │              Claims remaining: X/3
      │              ⏰ Cooldown: You can unclaim again in 1 hour."
      │
      └─────▶ END (User is now claimable by others)
```

---

## 💰 CONVERSION DETECTION FLOWCHART

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        CONVERSION DETECTION ENGINE                                   │
│                                                                                      │
│    Runs automatically via GuildMemberUpdate event listener                          │
│    Monitors: MAIN SERVER (1431718856619200686)                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘

EVENT: GuildMemberUpdate
      │
      │ Triggered when: Any member's roles change in Main Server
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              EVENT FILTERING                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── FILTER 1: Is this the Main Server?
      │     │
      │     │ if event.GuildID != "1431718856619200686"
      │     │
      │     ├─── NO ──▶ IGNORE EVENT
      │     └─── YES ─▶ CONTINUE
      │
      ├──── FILTER 2: Did user GAIN Paying Member role?
      │     │
      │     │ Compare: event.BeforeUpdate.Roles vs event.Member.Roles
      │     │ Check for: "885910828086362132" (Paying Member)
      │     │
      │     │ Logic:
      │     │ - Was NOT in BeforeUpdate.Roles
      │     │ - IS in Member.Roles (current)
      │     │
      │     ├─── DID NOT GAIN PAYING ROLE ──▶ IGNORE EVENT
      │     └─── GAINED PAYING ROLE ────────▶ POTENTIAL CONVERSION!
      │                                            │
      │                                            ▼
      └──── FILTER 3: Is this user claimed by an ambassador?
            │
            │ Query: SELECT * FROM claims
            │        WHERE claimed_user_id = event.user_id
            │        AND status = 'active'
            │
            ├─── NOT CLAIMED ──▶ IGNORE EVENT
            │                    (User upgraded on their own)
            │
            └─── IS CLAIMED ───▶ 🎉 CONVERSION DETECTED!
                                    │
                                    │ claim_record = query result
                                    │ ambassador_id = claim_record.ambassador_id
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              CONVERSION PROCESSING                                   │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Calculate Commission
      │  ──────────────────────────────
      │  Query: SELECT tier FROM ambassadors WHERE user_id = ambassador_id
      │
      │  Commission rates by tier:
      │  ┌──────────┬─────────────┐
      │  │ Tier     │ Commission  │
      │  ├──────────┼─────────────┤
      │  │ Bronze   │ $10.00      │
      │  │ Silver   │ $11.00      │
      │  │ Gold     │ $12.00      │
      │  │ Platinum │ $13.00      │
      │  └──────────┴─────────────┘
      │
      ▼
      │  STEP 2: Update Claim Status
      │  ─────────────────────────────
      │  UPDATE claims SET
      │      status = 'converted',
      │      converted_at = NOW()
      │  WHERE id = claim_record.id
      │
      ▼
      │  STEP 3: Insert Conversion Record
      │  ──────────────────────────────────
      │  INSERT INTO conversions (
      │      ambassador_id,
      │      converted_user_id,
      │      converted_username,
      │      converted_at,
      │      commission,
      │      claim_id,
      │      week_number,
      │      year
      │  ) VALUES (
      │      ambassador_id,
      │      event.user_id,
      │      event.username,
      │      NOW(),
      │      commission_amount,
      │      claim_record.id,
      │      EXTRACT(WEEK FROM NOW()),
      │      EXTRACT(YEAR FROM NOW())
      │  )
      │
      ▼
      │  STEP 4: Update Ambassador Stats & Balance
      │  ───────────────────────────────────────────
      │  UPDATE ambassadors SET
      │      total_conversions = total_conversions + 1,
      │      lifetime_conversions = lifetime_conversions + 1,
      │      current_claims = current_claims - 1,
      │      balance = balance + commission_amount,
      │      total_earned = total_earned + commission_amount
      │  WHERE user_id = ambassador_id
      │
      ▼
      │  STEP 5: Check & Update Tier
      │  ─────────────────────────────
      │  Query: SELECT lifetime_conversions FROM ambassadors
      │         WHERE user_id = ambassador_id
      │
      │  Tier thresholds:
      │  ┌──────────────────────┬──────────┐
      │  │ Lifetime Conversions │ Tier     │
      │  ├──────────────────────┼──────────┤
      │  │ 0-9                  │ Bronze   │
      │  │ 10-24                │ Silver   │
      │  │ 25-49                │ Gold     │
      │  │ 50+                  │ Platinum │
      │  └──────────────────────┴──────────┘
      │
      │  If tier changed:
      │  UPDATE ambassadors SET tier = new_tier
      │  WHERE user_id = ambassador_id
      │
      ▼
      │  STEP 6: Insert Balance Transaction
      │  ─────────────────────────────────────
      │  INSERT INTO balance_transactions (
      │      ambassador_id,
      │      type,
      │      amount,
      │      balance_after,
      │      description,
      │      created_at
      │  ) VALUES (
      │      ambassador_id,
      │      'conversion',
      │      +commission_amount,
      │      new_balance,
      │      'Conversion: @username',
      │      NOW()
      │  )
      │
      ▼
      │  STEP 7: Update Weekly Leaderboard
      │  ───────────────────────────────────
      │  INSERT INTO weekly_leaderboard (
      │      ambassador_id, week_number, year, conversions
      │  ) VALUES (
      │      ambassador_id, current_week, current_year, 1
      │  )
      │  ON CONFLICT (ambassador_id, week_number, year)
      │  DO UPDATE SET conversions = conversions + 1
      │
      ▼
      │  STEP 8: Notify Ambassador (Private Channel)
      │  ─────────────────────────────────────────────
      │  Query: SELECT private_channel_id FROM ambassadors
      │         WHERE user_id = ambassador_id
      │
      │  Post in ambassador's private channel:
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │ [conversion.png] BANNER                                        │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │                                                                 │
      │  │  🎉 **CONVERSION!**                                            │
      │  │                                                                 │
      │  │  Great news! **@ConvertedUser** just upgraded!                 │
      │  │                                                                 │
      │  │  **Commission Earned:** +$XX.XX                                │
      │  │  **New Balance:** $XX.XX                                       │
      │  │  **Total Conversions:** XX                                     │
      │  │  **Current Tier:** 🥈 Silver                                   │
      │  │                                                                 │
      │  │  Keep up the great work!                                       │
      │  │                                                                 │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │ [footer.png] FOOTER                                            │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 9: Log to Conversion Log Channel
      │  ───────────────────────────────────────
      │  Post embed to 1445214943841615922:
      │
      │  ┌─────────────────────────────────────────┐
      │  │ 💰 **CONVERSION RECORDED**              │
      │  │                                         │
      │  │ **Ambassador:** @Ambassador (ID)        │
      │  │ **Converted User:** @User (ID)          │
      │  │ **Commission:** $XX.XX                  │
      │  │ **Ambassador Total:** XX conversions    │
      │  │ **Timestamp:** <t:unix:F>               │
      │  │                                         │
      │  │ Color: Gold (#FFD700)                   │
      │  └─────────────────────────────────────────┘
      │
      ▼
      │  STEP 10: Update Leaderboard Embed (if exists)
      │  ───────────────────────────────────────────────
      │  If leaderboard message exists and is editable:
      │  - Re-query top 10
      │  - Edit message with new data
      │
      └─────▶ END (Conversion fully processed!)
```

---

## 💸 PAYOUT SYSTEM FLOWCHART

### Request Payout — COMPLETE FLOW

```
TRIGGER: Ambassador clicks "💰 Request Payout" button OR runs /request-payout
      │
      │ Executed in: AMBASSADOR SERVER (1441909666308423874) ONLY
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              ELIGIBILITY CHECKS                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── CHECK 1: IS USER AN AMBASSADOR?
      │     │
      │     │ Query: SELECT * FROM ambassadors
      │     │        WHERE user_id = ? AND is_active = true
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "You are not an active ambassador."
      │     └─── YES ─▶ CONTINUE (store ambassador record)
      │
      ├──── CHECK 2: HAS >= 2 TOTAL CONVERSIONS? (Unlock requirement)
      │     │
      │     │ if ambassador.lifetime_conversions < 2
      │     │
      │     ├─── < 2 CONVERSIONS
      │     │    │
      │     │    └──▶ ❌ EPHEMERAL: "You need at least 2 conversions to
      │     │                       unlock payouts. You have: X"
      │     │         └─── END
      │     │
      │     └─── >= 2 CONVERSIONS ─▶ CONTINUE
      │
      ├──── CHECK 3: BALANCE >= $10? (Minimum payout)
      │     │
      │     │ if ambassador.balance < 10.00
      │     │
      │     ├─── < $10
      │     │    │
      │     │    └──▶ ❌ EPHEMERAL: "Minimum payout amount is $10.00.
      │     │                       Your current balance: $X.XX"
      │     │         └─── END
      │     │
      │     └─── >= $10 ─▶ CONTINUE
      │
      └──── CHECK 4: NO PENDING PAYOUT REQUEST?
            │
            │ Query: SELECT * FROM payout_requests
            │        WHERE ambassador_id = ?
            │        AND status = 'pending'
            │
            ├─── HAS PENDING
            │    │
            │    └──▶ ❌ EPHEMERAL: "You already have a pending payout request.
            │                       Please wait for it to be processed."
            │         └─── END
            │
            └─── NO PENDING ─▶ ✅ ELIGIBLE FOR PAYOUT!
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              PAYOUT MODAL                                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  Show Modal:
      │  ───────────
      │  Title: "Request Payout"
      │  CustomID: "Payout:RequestModal"
      │
      │  ┌─────────────────────────────────────────────────────────────────────────┐
      │  │                                                                         │
      │  │  📝 Field 1: Payout Amount                                             │
      │  │  ─────────────────────────────                                         │
      │  │  Label: "How much would you like to withdraw? (Balance: $XX.XX)"       │
      │  │  Placeholder: "Enter amount (e.g., 25.00)"                             │
      │  │  Style: TextInputShort                                                 │
      │  │  Required: true                                                        │
      │  │  MinLength: 1                                                          │
      │  │  MaxLength: 10                                                         │
      │  │                                                                         │
      │  │  ────────────────────────────────────────────                          │
      │  │                                                                         │
      │  │  📝 Field 2: Payment Method                                            │
      │  │  ────────────────────────────                                          │
      │  │  Label: "Preferred Payment Method"                                     │
      │  │  Placeholder: "PayPal email address or other payment details"          │
      │  │  Style: TextInputParagraph                                             │
      │  │  Required: true                                                        │
      │  │  MinLength: 5                                                          │
      │  │  MaxLength: 200                                                        │
      │  │                                                                         │
      │  └─────────────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  User submits modal
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              MODAL VALIDATION                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── VALIDATE: Amount is a valid number?
      │     │
      │     │ amount, err := strconv.ParseFloat(input, 64)
      │     │
      │     ├─── INVALID ──▶ ❌ EPHEMERAL: "Please enter a valid amount."
      │     └─── VALID ────▶ CONTINUE
      │
      ├──── VALIDATE: Amount >= $10?
      │     │
      │     ├─── < $10 ──▶ ❌ EPHEMERAL: "Minimum payout is $10.00."
      │     └─── >= $10 ─▶ CONTINUE
      │
      └──── VALIDATE: Amount <= Balance?
            │
            ├─── > BALANCE ──▶ ❌ EPHEMERAL: "Amount exceeds your balance ($XX.XX)."
            └─── <= BALANCE ─▶ ✅ PROCEED TO CREATE REQUEST
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              PAYOUT REQUEST CREATION                                 │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Create Private Thread
      │  ───────────────────────────────
      │  s.ThreadStartComplex(
      │      ChannelID: panel_channel_id,  // Where /panel was posted
      │      ThreadStart{
      │          Name: "[PAYOUT] @Username — $XX.XX",
      │          Type: GuildPrivateThread,
      │          Invitable: false,
      │          AutoArchiveDuration: 10080
      │      }
      │  )
      │
      ▼
      │  STEP 2: Add Members to Thread
      │  ───────────────────────────────
      │  s.ThreadMemberAdd(thread_id, ambassador_user_id)
      │  // Staff can see all private threads by default
      │
      ▼
      │  STEP 3: Insert Payout Request
      │  ───────────────────────────────
      │  INSERT INTO payout_requests (
      │      ambassador_id,
      │      amount,
      │      payment_method,
      │      thread_id,
      │      status,
      │      requested_at
      │  ) VALUES (
      │      ambassador_id,
      │      amount,
      │      payment_method_input,
      │      thread_id,
      │      'pending',
      │      NOW()
      │  )
      │
      ▼
      │  STEP 4: Post Payout Request Embed in Thread
      │  ─────────────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │                                                                 │
      │  │  # 💰 Payout Request                                           │
      │  │                                                                 │
      │  │  **Ambassador:** @Username                                      │
      │  │  **Amount Requested:** $XX.XX                                  │
      │  │  **Current Balance:** $XX.XX                                   │
      │  │  **Payment Method:** PayPal: email@example.com                 │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  **Ambassador Stats:**                                         │
      │  │  • Total Conversions: XX                                       │
      │  │  • Lifetime Earnings: $XX.XX                                   │
      │  │  • Total Paid Out: $XX.XX                                      │
      │  │  • Current Tier: 🥈 Silver                                     │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  ⚠️ Staff: Send payment via specified method,                   │
      │  │  then click the button below to complete.                      │
      │  │                                                                 │
      │  │  ┌───────────────────────┐                                     │
      │  │  │ ✅ Complete Payout    │  CustomID: "Payout:Complete"        │
      │  │  └───────────────────────┘                                     │
      │  │                                                                 │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 5: Respond to Ambassador
      │  ───────────────────────────────
      │  EPHEMERAL: "✅ Payout request submitted!
      │              
      │              Amount: $XX.XX
      │              Thread: [link to thread]
      │              
      │              Staff will process your request shortly."
      │
      └─────▶ END (Waiting for admin to process)


══════════════════════════════════════════════════════════════════════════════
                         ADMIN COMPLETES PAYOUT
══════════════════════════════════════════════════════════════════════════════

TRIGGER: Staff clicks "✅ Complete Payout" button
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              ADMIN VALIDATION                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── CHECK: Is user Staff/Admin?
      │     │
      │     ├─── NO ──▶ ❌ EPHEMERAL: "Only staff can complete payouts."
      │     └─── YES ─▶ CONTINUE
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              COMPLETION MODAL                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  Show Modal:
      │  ───────────
      │  Title: "Complete Payout"
      │  CustomID: "Payout:CompleteModal"
      │
      │  ┌─────────────────────────────────────────────────────────────────────────┐
      │  │                                                                         │
      │  │  📝 Field 1: Actual Amount Paid                                        │
      │  │  ──────────────────────────────                                        │
      │  │  Label: "How much did you actually pay to this ambassador?"            │
      │  │  Placeholder: "Enter exact amount sent (e.g., 25.00)"                  │
      │  │  Style: TextInputShort                                                 │
      │  │  Required: true                                                        │
      │  │                                                                         │
      │  └─────────────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  Admin submits modal
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              PAYOUT COMPLETION                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Get Payout Request from Thread
      │  ────────────────────────────────────────
      │  Query: SELECT * FROM payout_requests
      │         WHERE thread_id = current_thread_id
      │         AND status = 'pending'
      │
      ▼
      │  STEP 2: Update Payout Request
      │  ───────────────────────────────
      │  UPDATE payout_requests SET
      │      status = 'completed',
      │      completed_at = NOW(),
      │      completed_by = admin_id,
      │      actual_amount = input_amount
      │  WHERE id = request_id
      │
      ▼
      │  STEP 3: Update Ambassador Balance
      │  ───────────────────────────────────
      │  UPDATE ambassadors SET
      │      balance = balance - actual_amount,
      │      total_paid_out = total_paid_out + actual_amount
      │  WHERE user_id = ambassador_id
      │
      ▼
      │  STEP 4: Insert Balance Transaction
      │  ─────────────────────────────────────
      │  INSERT INTO balance_transactions (
      │      ambassador_id,
      │      type,
      │      amount,
      │      balance_after,
      │      description,
      │      performed_by,
      │      created_at
      │  ) VALUES (
      │      ambassador_id,
      │      'payout',
      │      -actual_amount,  // Negative for debit
      │      new_balance,
      │      'Payout completed by @Admin',
      │      admin_id,
      │      NOW()
      │  )
      │
      ▼
      │  STEP 5: Notify Ambassador (Private Channel)
      │  ─────────────────────────────────────────────
      │  Post in ambassador's :bell:｜[name] channel:
      │
      │  "💸 **Payout Complete!**
      │   
      │   Amount: $XX.XX
      │   Processed by: @Admin
      │   New Balance: $XX.XX
      │   
      │   Thank you for your contributions!"
      │
      ▼
      │  STEP 6: DELETE Payout Thread
      │  ──────────────────────────────
      │  s.ChannelDelete(thread_id)
      │
      ▼
      │  STEP 7: Respond to Admin
      │  ─────────────────────────
      │  EPHEMERAL: "✅ Payout of $XX.XX completed for @Ambassador.
      │              Thread closed. Ambassador notified."
      │
      └─────▶ END
```

---

## 🏆 LEADERBOARD SYSTEM FLOWCHART

### /leaderboard — Admin Posts Leaderboard

```
/leaderboard
      │
      │ Executed by: Admin ONLY
      │ Server: AMBASSADOR SERVER (1441909666308423874) ONLY
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              VALIDATION                                              │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── CHECK 1: Is executor Staff/Admin?
      │     ├─── NO ──▶ ❌ EPHEMERAL: "Only administrators can use this command."
      │     └─── YES ─▶ CONTINUE
      │
      └──── CHECK 2: Correct server?
            ├─── NO ──▶ ❌ EPHEMERAL: "This command only works in Ambassador Server."
            └─── YES ─▶ CONTINUE
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              BUILD LEADERBOARD                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Query Weekly Top 10
      │  ─────────────────────────────
      │  SELECT 
      │      a.user_id,
      │      a.username,
      │      COALESCE(w.conversions, 0) as weekly_conversions
      │  FROM ambassadors a
      │  LEFT JOIN weekly_leaderboard w ON a.user_id = w.ambassador_id 
      │      AND w.week_number = EXTRACT(WEEK FROM CURRENT_DATE)
      │      AND w.year = EXTRACT(YEAR FROM CURRENT_DATE)
      │  WHERE a.is_active = true
      │  ORDER BY weekly_conversions DESC, a.username ASC  // Alpha sort for ties
      │  LIMIT 10
      │
      ▼
      │  STEP 2: Format Leaderboard Text
      │  ─────────────────────────────────
      │  For each rank:
      │
      │  Rank 1: "# 🥇 1. @Username — XX conversions"     (H1 - largest)
      │  Rank 2: "## 🥈 2. @Username — XX conversions"    (H2 - large)
      │  Rank 3: "### 🥉 3. @Username — XX conversions"   (H3 - medium)
      │  Rank 4-10: "4. @Username — XX conversions"       (regular text)
      │
      │  If no conversions this week:
      │  "No conversions recorded this week yet. Be the first!"
      │
      ▼
      │  STEP 3: Build Embed
      │  ─────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │ [leaderboard.png] BANNER                                       │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │                                                                 │
      │  │  # 🏆 Weekly Leaderboard                                       │
      │  │                                                                 │
      │  │  Top ambassadors by conversions this week.                     │
      │  │  Resets every Sunday at midnight UTC.                          │
      │  │                                                                 │
      │  │  ═══════════════════════════════════════                       │
      │  │                                                                 │
      │  │  # 🥇 1. @TopAmbassador — 15 conversions                       │
      │  │                                                                 │
      │  │  ## 🥈 2. @SecondPlace — 12 conversions                        │
      │  │                                                                 │
      │  │  ### 🥉 3. @ThirdPlace — 10 conversions                        │
      │  │                                                                 │
      │  │  4. @FourthPlace — 8 conversions                               │
      │  │  5. @FifthPlace — 7 conversions                                │
      │  │  ...                                                           │
      │  │                                                                 │
      │  │  ═══════════════════════════════════════                       │
      │  │                                                                 │
      │  │  ┌─────────────────────────────────────┐                       │
      │  │  │ 🏅 Lifetime Leaderboard             │                       │
      │  │  └─────────────────────────────────────┘                       │
      │  │  CustomID: "Leaderboard:ViewLifetime"                          │
      │  │                                                                 │
      │  ├─────────────────────────────────────────────────────────────────┤
      │  │ [footer.png] FOOTER                                            │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      ▼
      │  STEP 4: Send Message (Bot sends directly)
      │  ───────────────────────────────────────────
      │  s.ChannelMessageSendComplex(channelID, message)
      │
      ▼
      │  STEP 5: Store Message ID (for updates)
      │  ─────────────────────────────────────────
      │  Store message_id and channel_id for future updates
      │  (could be in DB or config)
      │
      ▼
      │  STEP 6: Respond to Admin
      │  ─────────────────────────
      │  EPHEMERAL: "✅ Leaderboard posted successfully."
      │
      └─────▶ END


══════════════════════════════════════════════════════════════════════════════
                      LIFETIME LEADERBOARD BUTTON
══════════════════════════════════════════════════════════════════════════════

TRIGGER: User clicks "🏅 Lifetime Leaderboard" button
      │
      │ CustomID: "Leaderboard:ViewLifetime"
      │
      ▼
      │  STEP 1: Query Lifetime Top 10
      │  ───────────────────────────────
      │  SELECT 
      │      user_id,
      │      username,
      │      lifetime_conversions
      │  FROM ambassadors
      │  WHERE is_active = true
      │  ORDER BY lifetime_conversions DESC, username ASC
      │  LIMIT 10
      │
      ▼
      │  STEP 2: Format & Respond (EPHEMERAL)
      │  ──────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────┐
      │  │ 🏅 **Lifetime Leaderboard**             │
      │  │                                         │
      │  │ All-time top ambassadors:               │
      │  │                                         │
      │  │ 🥇 1. @User — 150 conversions           │
      │  │ 🥈 2. @User — 120 conversions           │
      │  │ 🥉 3. @User — 100 conversions           │
      │  │ 4. @User — 85 conversions               │
      │  │ 5. @User — 70 conversions               │
      │  │ ...                                     │
      │  │                                         │
      │  │ Updated: <t:unix:R>                     │
      │  └─────────────────────────────────────────┘
      │
      └─────▶ END


══════════════════════════════════════════════════════════════════════════════
                         WEEKLY RESET MECHANISM
══════════════════════════════════════════════════════════════════════════════

TRIGGER: Scheduled task — Sunday 00:00 UTC
      │
      │ Implementation: time.Ticker or cron job
      │
      ▼
      │  STEP 1: Archive Current Week's Leaders (Optional)
      │  ───────────────────────────────────────────────────
      │  Could announce winners in a channel before reset
      │
      ▼
      │  STEP 2: Reset Weekly Conversions
      │  ──────────────────────────────────
      │  UPDATE ambassadors SET
      │      total_conversions = 0
      │  WHERE is_active = true
      │
      │  Note: lifetime_conversions is NOT reset
      │
      ▼
      │  STEP 3: Clear Weekly Leaderboard Table
      │  ─────────────────────────────────────────
      │  DELETE FROM weekly_leaderboard
      │  WHERE week_number = last_week AND year = last_year
      │
      │  OR: Keep historical data, just insert new week
      │
      ▼
      │  STEP 4: Update Leaderboard Embed
      │  ──────────────────────────────────
      │  If stored message exists:
      │  s.ChannelMessageEdit(channel_id, message_id, new_content)
      │
      │  Shows: "No conversions recorded this week yet."
      │
      ▼
      │  STEP 5: Log Reset
      │  ───────────────────
      │  Post to Conversion Log:
      │  "🔄 **WEEKLY RESET** — Leaderboard reset for new week."
      │
      └─────▶ END
```

---

## 📊 PANEL BUTTON FLOWS

### "📊 My Stats" Button

```
TRIGGER: Ambassador clicks "📊 My Stats" button
      │
      │ CustomID: "Panel:ViewStats"
      │
      ▼
      │  STEP 1: Verify User is Ambassador
      │  ───────────────────────────────────
      │  Query: SELECT * FROM ambassadors WHERE user_id = ? AND is_active = true
      │
      │  ├─── NOT FOUND ──▶ ❌ EPHEMERAL: "You are not an ambassador."
      │  └─── FOUND ──────▶ CONTINUE
      │
      ▼
      │  STEP 2: Build Stats Response (EPHEMERAL)
      │  ──────────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │                                                                 │
      │  │  📊 **Your Ambassador Stats**                                  │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  **Claims:**                                                   │
      │  │  • Current Active: 2/3                                         │
      │  │  • Total Claims Made: 45                                       │
      │  │  • Total Unclaims: 12                                          │
      │  │                                                                 │
      │  │  **Conversions:**                                              │
      │  │  • This Week: 3                                                │
      │  │  • Lifetime: 28                                                │
      │  │  • Conversion Rate: 62.2%                                      │
      │  │    (conversions / total_claims × 100)                          │
      │  │                                                                 │
      │  │  **Financials:**                                               │
      │  │  • Current Balance: $125.00                                    │
      │  │  • Total Earned: $280.00                                       │
      │  │  • Total Paid Out: $155.00                                     │
      │  │                                                                 │
      │  │  **Tier:** 🥈 Silver                                           │
      │  │  (10+ lifetime conversions)                                    │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  Next tier: 🥇 Gold at 25 conversions                          │
      │  │  Progress: ████████░░░░ 28/25 ✅ UNLOCKED!                      │
      │  │                                                                 │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      └─────▶ END
```

### "👥 My Claims" Button

```
TRIGGER: Ambassador clicks "👥 My Claims" button
      │
      │ CustomID: "Panel:ViewClaims"
      │
      ▼
      │  STEP 1: Verify User is Ambassador
      │  ───────────────────────────────────
      │  ├─── NOT FOUND ──▶ ❌ EPHEMERAL: "You are not an ambassador."
      │  └─── FOUND ──────▶ CONTINUE
      │
      ▼
      │  STEP 2: Query Active Claims
      │  ─────────────────────────────
      │  SELECT * FROM claims
      │  WHERE ambassador_id = user_id
      │  AND status = 'active'
      │  ORDER BY claimed_at DESC
      │
      ▼
      │  STEP 3: Build Claims List (EPHEMERAL with buttons)
      │  ────────────────────────────────────────────────────
      │
      │  ┌─────────────────────────────────────────────────────────────────┐
      │  │                                                                 │
      │  │  👥 **Your Active Claims** (2/3)                               │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  1. **@Username1**                                             │
      │  │     Claimed: <t:unix:R> (3 days ago)                           │
      │  │     ┌────────────┐                                             │
      │  │     │ ❌ Unclaim │  CustomID: "Claims:Unclaim:user_id_1"       │
      │  │     └────────────┘                                             │
      │  │                                                                 │
      │  │  2. **@Username2**                                             │
      │  │     Claimed: <t:unix:R> (1 week ago)                           │
      │  │     ┌────────────┐                                             │
      │  │     │ ❌ Unclaim │  CustomID: "Claims:Unclaim:user_id_2"       │
      │  │     └────────────┘                                             │
      │  │                                                                 │
      │  │  ─────────────────────────────────────                         │
      │  │                                                                 │
      │  │  ⏰ Unclaim cooldown: Ready / X minutes remaining              │
      │  │                                                                 │
      │  │  💡 Tip: Choose wisely! Unclaimed users can be                 │
      │  │  claimed by other ambassadors.                                 │
      │  │                                                                 │
      │  └─────────────────────────────────────────────────────────────────┘
      │
      │  If no active claims:
      │  "You don't have any active claims. Use /claim in the Main Server!"
      │
      └─────▶ END


══════════════════════════════════════════════════════════════════════════════
                    UNCLAIM FROM MY CLAIMS PANEL
══════════════════════════════════════════════════════════════════════════════

TRIGGER: Ambassador clicks "❌ Unclaim" button on specific claim
      │
      │ CustomID: "Claims:Unclaim:{user_id}"
      │
      ▼
      │  Parse user_id from CustomID
      │
      ▼
      │  Run same validation as /unclaim command:
      │  • Is ambassador on cooldown?
      │  • Does ambassador have this claim?
      │
      │  If passes: Execute unclaim (same as /unclaim flow)
      │  If fails: Show error ephemeral
      │
      └─────▶ END
```

---

## 🔧 ADMIN COMMANDS FLOWCHART

### /balance-add <user> <amount>

```
/balance-add @Ambassador $50.00
      │
      │ Admin ONLY | Ambassador Server ONLY
      │
      ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              VALIDATION                                              │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      ├──── Is executor Staff/Admin?
      │     ├─── NO ──▶ ❌ EPHEMERAL: "Only administrators can use this command."
      │     └─── YES ─▶ CONTINUE
      │
      ├──── Is target an active ambassador?
      │     ├─── NO ──▶ ❌ EPHEMERAL: "User is not an active ambassador."
      │     └─── YES ─▶ CONTINUE
      │
      └──── Is amount valid positive number?
            ├─── NO ──▶ ❌ EPHEMERAL: "Please enter a valid positive amount."
            └─── YES ─▶ CONTINUE
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              EXECUTION                                               │
└─────────────────────────────────────────────────────────────────────────────────────┘
      │
      │  STEP 1: Update Ambassador Balance
      │  ───────────────────────────────────
      │  UPDATE ambassadors SET
      │      balance = balance + amount,
      │      total_earned = total_earned + amount
      │  WHERE user_id = target_id
      │
      ▼
      │  STEP 2: Insert Balance Transaction
      │  ─────────────────────────────────────
      │  INSERT INTO balance_transactions (
      │      ambassador_id, type, amount, balance_after,
      │      description, performed_by, created_at
      │  ) VALUES (
      │      target_id, 'bonus', +amount, new_balance,
      │      'Manual balance addition by @Admin',
      │      admin_id, NOW()
      │  )
      │
      ▼
      │  STEP 3: Notify Ambassador (Private Channel)
      │  ─────────────────────────────────────────────
      │  Post in ambassador's :bell:｜[name] channel:
      │
      │  "💵 **Balance Updated**
      │   
      │   An admin has added **+$XX.XX** to your balance.
      │   
      │   New Balance: $XX.XX
      │   Updated by: @Admin"
      │
      ▼
      │  STEP 4: Respond to Admin
      │  ─────────────────────────
      │  EPHEMERAL: "✅ Added $XX.XX to @Ambassador's balance.
      │              New balance: $XX.XX"
      │
      └─────▶ END
```

### /balance-remove <user> <amount>

```
/balance-remove @Ambassador $20.00
      │
      │ Admin ONLY | Ambassador Server ONLY
      │ (Same flow as /balance-add but with subtraction)
      │
      ▼
      │  Additional validation: amount <= current balance
      │  (Cannot go negative)
      │
      ▼
      │  UPDATE ambassadors SET
      │      balance = balance - amount
      │  WHERE user_id = target_id
      │
      ▼
      │  INSERT transaction with type = 'adjustment', amount = -XX
      │
      ▼
      │  Notify ambassador: "An admin has removed $XX.XX from your balance."
      │
      └─────▶ END
```

---

## 🌳 COMPLETE SYSTEM SUMMARY TREE

```
AMBASSADOR SYSTEM
│
├─── 🏛️ INFRASTRUCTURE
│    ├─── Main Server (1431718856619200686)
│    │    ├─── Commands: /claim, /unclaim
│    │    ├─── Events: GuildMemberUpdate (conversion detection)
│    │    └─── Roles: Ambassador, Paying, Lifetime, Default
│    │
│    ├─── Ambassador Server (1441909666308423874)
│    │    ├─── Admin Commands: /wait-list, /owls-vision, /onboard, /leaderboard,
│    │    │                    /panel, /balance-add, /balance-remove, /ambassador-stats
│    │    ├─── Ambassador Commands: /my-stats, /my-claims, /request-payout
│    │    ├─── Channels: Approved Applicants, Private Channels, Claim Log, Conversion Log
│    │    └─── Roles: Staff, Approved Owl, Full Ambassador
│    │
│    └─── Bot Requirements
│         ├─── Present in BOTH servers
│         ├─── Highest role in hierarchy
│         └─── Intents: Guilds, GuildMembers, GuildMessages
│
├─── 📊 DATABASE (PostgreSQL)
│    ├─── ambassador_applicants (pending interviews)
│    ├─── ambassadors (stats, balance, tier)
│    ├─── claims (claim relationships)
│    ├─── conversions (conversion history)
│    ├─── payout_requests (payout tracking)
│    ├─── balance_transactions (audit log)
│    └─── weekly_leaderboard (weekly stats)
│
├─── 👥 USER JOURNEYS
│    │
│    ├─── APPLICANT JOURNEY
│    │    │
│    │    ├─── 1. Discovery
│    │    │    └─── Sees /wait-list embed → Clicks "Apply Now"
│    │    │
│    │    ├─── 2. Application
│    │    │    └─── Fills Tally form → Admin reviews externally
│    │    │
│    │    ├─── 3. Onboarding
│    │    │    └─── Admin runs /onboard → Gets Approved Owl role
│    │    │         → Interview thread created
│    │    │
│    │    ├─── 4. Interview
│    │    │    ├─── REJECTED → Role removed, thread deleted
│    │    │    └─── ACCEPTED → Full Ambassador role, private channel created
│    │    │
│    │    └─── 5. Active Ambassador
│    │         └─── Can now claim, convert, earn commissions
│    │
│    ├─── CLAIM JOURNEY
│    │    │
│    │    ├─── 1. Ambassador finds free member in Main Server
│    │    │
│    │    ├─── 2. Runs /claim @user
│    │    │    └─── 6 validation checks → Claim recorded
│    │    │
│    │    ├─── 3. Guides member toward paid membership
│    │    │
│    │    ├─── 4. Member upgrades (gains Paying role)
│    │    │    └─── GuildMemberUpdate event triggers
│    │    │
│    │    └─── 5. Conversion processed
│    │         └─── Stats updated, commission added, notification sent
│    │
│    └─── PAYOUT JOURNEY
│         │
│         ├─── 1. Ambassador has >= 2 conversions, >= $10 balance
│         │
│         ├─── 2. Clicks "Request Payout" or /request-payout
│         │    └─── Modal: amount + payment method
│         │
│         ├─── 3. Payout thread created
│         │
│         ├─── 4. Admin sends payment externally
│         │
│         ├─── 5. Admin clicks "Complete Payout"
│         │    └─── Modal: actual amount paid
│         │
│         └─── 6. Balance deducted, thread deleted, ambassador notified
│
├─── 🖼️ EMBEDS (8 unique designs)
│    ├─── waitlist.png → /wait-list
│    ├─── ourvision.png → /owls-vision
│    ├─── leaderboard.png → /leaderboard
│    ├─── panel.png → /panel
│    ├─── ambassadorwelcome.png → Private channel welcome
│    ├─── approved.png → Acceptance notification
│    ├─── conversion.png → Conversion notification
│    └─── footer.png → All embeds
│
├─── ⏰ SCHEDULED TASKS
│    └─── Weekly Reset (Sunday 00:00 UTC)
│         ├─── Reset total_conversions in ambassadors table
│         ├─── Archive/clear weekly_leaderboard
│         └─── Update leaderboard embed
│
└─── 📝 LOGGING
     ├─── Claim Log (1445215326550622419)
     │    └─── All claims and unclaims
     │
     ├─── Conversion Log (1445214943841615922)
     │    └─── All conversions, new ambassadors, weekly resets
     │
     └─── Ambassador Private Channels
          └─── Balance changes, payouts, tier upgrades
```

---

## 📐 INTERACTION ID REFERENCE

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                              CUSTOM IDs MASTER LIST                                   │
└──────────────────────────────────────────────────────────────────────────────────────┘

INTERVIEW SYSTEM
├── Interview:Accept ────────────── Accept applicant button
├── Interview:Reject ────────────── Reject applicant button
├── Interview:ConfirmAccept ─────── Confirmation button
├── Interview:ConfirmReject ─────── Confirmation button
└── Interview:Cancel ────────────── Cancel action button

PANEL SYSTEM
├── Panel:ViewStats ─────────────── My Stats button
├── Panel:ViewClaims ────────────── My Claims button
└── Panel:RequestPayout ─────────── Request Payout button

CLAIMS SYSTEM
└── Claims:Unclaim:{user_id} ────── Unclaim specific user from panel

PAYOUT SYSTEM
├── Payout:RequestModal ─────────── Request payout modal
├── Payout:Complete ─────────────── Complete payout button (admin)
└── Payout:CompleteModal ────────── Completion modal (admin)

LEADERBOARD SYSTEM
└── Leaderboard:ViewLifetime ────── View lifetime leaderboard button
```

---

> **End of Flowchart Document**
> 
> This flowchart covers every interaction, validation, database operation,
> and user journey in the Ambassador System.

