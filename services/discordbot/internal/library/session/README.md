# Session Utilities

- `session_config.go`: monta sessão com intents, logging e ratelimit.
- `reconnect_strategy.go`: ajustes de reconnect/heartbeat.
- `utilities.go`: helpers para IDs/timestamps do Discord.

Use estas funções como base ao construir o `main.go` ou scripts customizados.
