# OAuth & Linked Roles

- `/library-oauth-link`: gera URL de autorização com escopos personalizados.
- `linked_roles_metadata.go`: função utilitária para atualizar metadados de linked roles (necessita token da aplicação).

Use estes exemplos como base para integrar login externo ou recursos de role connections.
