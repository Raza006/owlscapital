# Interaction Examples

Inclui:
- Slash (`/library-button-basic`, `/library-autocomplete-role`, `/library-modal-feedback`, `/library-select-multi`, `/library-localized`).
- Context menus (`Library: Ver Perfil`, `Library: Citar`).
- Componentes avançados e modals.

Ative intents de guild e registre os comandos via `features_enabled_library.go` para testar.
